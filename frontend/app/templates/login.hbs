{{page-title "Login"}}
<div class="flex h-full bg-base-200">
  <div class="m-auto">
    <div class="card w-96 bg-base-100 shadow-xl">
      <div class="card-body">
        <h2 class="card-title justify-center pb-5">
          login with facebook or github
        </h2>
        <div class="flex flex-row space-x-5 justify-center">
          <LinkTo @route="dashboard" class="btn btn-primary px-5">
            <FaIcon @icon="facebook" @prefix="fa-brands" @size="2x" />
          </LinkTo>
          <LinkTo @route="dashboard" class="btn btn-primary px-5">
            <FaIcon @icon="github" @prefix="fa-brands" @size="2x" />
          </LinkTo>
        </div>
      </div>
    </div>
  </div>
</div>